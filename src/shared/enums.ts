export enum ActionType {
    CAST = 'CAST',
    OPPONENT_CAST = 'OPPONENT_CAST',
    LEARN = 'LEARN',
    BREW = 'BREW',
    WAIT = 'WAIT',
    REST = 'REST',
}

export enum League {
    Wood1,
    Bronze,
}
